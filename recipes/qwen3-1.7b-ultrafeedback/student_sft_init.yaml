# Student SFT init — Qwen3-1.7B-Base supervised-fine-tuned on Deita-10k.
# Same no-thinking ChatML template as the teacher SFT (hard-coded, overrides the
# base model's). This is the "SFT" row in the results table and the init for all
# the distillation / DPO methods.
model_name_or_path: Qwen/Qwen3-1.7B-Base
chat_template: "{% for message in messages %}{{ '<|im_start|>' + message['role'] + '\n' + message['content'] | trim + '<|im_end|>' + '\n' }}{% endfor %}{% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% endif %}"
model_revision: main
torch_dtype: bfloat16

# Data
dataset_mixer:
  HuggingFaceH4/deita-10k-v0-sft: 1.0
dataset_splits:
- train_sft
- test_sft
preprocessing_num_workers: 12
packing: true

# SFT trainer config
bf16: true
dataset_kwargs:
  add_special_tokens: false
  append_concat_token: false
do_eval: false
gradient_accumulation_steps: 1
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
learning_rate: 3.0e-05
log_level: info
logging_steps: 5
logging_strategy: steps
lr_scheduler_type: cosine
max_seq_length: 2048
max_steps: -1
num_train_epochs: 1
optim: adamw_torch
output_dir: /workspace/riskKD/output/qwen3-1.7b-deita-sft-student
per_device_eval_batch_size: 8
per_device_train_batch_size: 32
push_to_hub: false
remove_unused_columns: true
report_to: wandb
run_name: qwen3-1.7b-sft-student
save_strategy: "epoch"
save_only_model: true
seed: 42
warmup_ratio: 0.1
