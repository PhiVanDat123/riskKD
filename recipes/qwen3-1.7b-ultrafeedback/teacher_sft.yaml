# Teacher SFT — Qwen3-8B-Base supervised-fine-tuned on Deita-10k.
# No-thinking: a plain ChatML template (no <think> blocks), hard-coded here so it
# overrides whatever the base model ships. Qwen has no BOS token, so the template
# doesn't prepend one (unlike the Llama recipes). Mirrors the structure of the
# Llama recipes/.../teacher_sft.yaml.
model_name_or_path: Qwen/Qwen3-8B-Base
chat_template: "{% for message in messages %}{{ '<|im_start|>' + message['role'] + '\n' + message['content'] | trim + '<|im_end|>' + '\n' }}{% endfor %}{% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% endif %}"
model_revision: main
torch_dtype: bfloat16
use_flash_attention_2: false

# Data
dataset_mixer:
  HuggingFaceH4/deita-10k-v0-sft: 1.0
dataset_splits:
- train_sft
- test_sft
preprocessing_num_workers: 12
packing: true

# SFT trainer config
bf16: true
dataset_kwargs:
  add_special_tokens: false
  append_concat_token: false
do_eval: false
gradient_accumulation_steps: 1
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
learning_rate: 2.5e-05
log_level: info
logging_steps: 5
logging_strategy: steps
lr_scheduler_type: cosine
max_seq_length: 2048
max_steps: -1
num_train_epochs: 1
optim: adamw_torch
output_dir: /workspace/riskKD/output/qwen3-8b-deita-sft-teacher
per_device_eval_batch_size: 1
per_device_train_batch_size: 16
push_to_hub: false
remove_unused_columns: true
report_to: wandb
run_name: qwen3-8b-sft-teacher
save_strategy: "epoch"
save_only_model: true
seed: 42
warmup_ratio: 0.1
